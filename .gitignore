site/
__pycache__/
*.pyc
.venv/
.env
.scratch/

# Output of the wiki-monitor skill, written into the working directory by default.
# Digests are dated (digest-YYYY-MM-DD.html) so they accumulate rather than
# overwrite; they are meant to be read, not versioned.
digest-*.html
digest.html
candidates.json
findings.json
